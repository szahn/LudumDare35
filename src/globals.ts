class Globals{
    static TILE_SIZE = 20;
    static CANVAS_WIDTH = 640;
    static CANVAS_HEIGHT = 480;
    static CANVAS_WIDTH_HALF = Globals.CANVAS_WIDTH / 2;
    static CANVAS_HEIGHT_HALF = Globals.CANVAS_HEIGHT / 2;
}

export = Globals;